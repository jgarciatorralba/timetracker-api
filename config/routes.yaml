# FAVICON (avoid console error)
favicon:
  path: /favicon.ico
  controller:
  methods: GET

# USERS
users_get_all:
    path: /users
    controller: App\UI\Controller\Users\GetUsersController
    methods: GET

# WORK ENTRIES
